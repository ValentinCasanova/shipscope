# Infrastructure

Terraform for ShipScope's AWS environments (staging and prod): ECR, ECS Fargate behind an ALB, RDS Postgres, S3 + CloudFront, and Secrets Manager.

Not yet implemented — this lands with the deployment pipeline work.
